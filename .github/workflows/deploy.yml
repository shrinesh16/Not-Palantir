name: Deploy Mythrion Dashboard

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: mythrion-dashboard/package-lock.json

      - name: Install Dependencies
        run: |
          cd mythrion-dashboard
          npm ci

      - name: Build Project
        run: |
          cd mythrion-dashboard
          npm run build

      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: mythrion-dashboard/dist
          branch: gh-pages
